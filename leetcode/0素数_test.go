package leetcode

import (
	"fmt"
	"testing"
)

func TestPrimeCount(t *testing.T) {
	n := 100
	fmt.Println(PrimeCount(n))
}

func TestEratosthenes(t *testing.T) {
	n := 100
	fmt.Println(Eratosthenes(n))
}
